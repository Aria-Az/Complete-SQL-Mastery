-- Foreign Keys

-- A FOREIGN KEY is a field (or collection of fields) in one table,
-- that refers to the PRIMARY KEY in another table.

-- A composite key is a specific type of primary key which
-- uses the contents of two or more fields from a table to create a unique value
-- We used composite key (student_id and course_id) for enrollments table