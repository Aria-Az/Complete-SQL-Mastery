-- Fixed-point and Floating-point Types

-- DECIMAL(p, s)     fixed digits after decimal point
-- DECIMAL(9, 2) => 1234567.89

-- DECIMAL = DEC = NUMERIC = FIXED

-- FLOAT   4b  Very large or very small numbers (they dont store the exact value)
-- DOUBLE  8b  Very large or very small numbers (they dont store the exact value)

